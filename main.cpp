#include <iostream>
#include <string>
#include <time.h>

using namespace std;


class SearchResult{
    private:
    string searchPrompt;
    struct published_time; 

    public:
    SearchResult(string searchPrompt);

};

SearchResult::SearchResult(string searchPrompt){
    
}